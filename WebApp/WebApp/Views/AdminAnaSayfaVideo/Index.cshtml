@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutWebAdmin.cshtml";
    string baslik = string.Empty;
}
<section class="wrapper">
    <div class="row">
        <div class="col-lg-12">
            <section class="panel">
                <header class="panel-heading">
                    @if (ViewBag.FotografList != null)
                    {
                        baslik = " Ana Sayfa Video Listesi" + "-" + " " + " " + "Kayıt Sayısı:" + ViewBag.FotografList.Count.ToString();
                    }
                    else
                    {
                        baslik = " Ana Sayfa Video Listesi" + "-" + "Kayıt Sayısı:0";
                    }
                    @baslik

                </header>
                <div class="panel-body">
                    <section id="unseen">
                        <div class="adv-table">
                            <a class="btn btn-primary" id="btnEkle" href="/AdminAnaSayfaVideoEkle/"><i class="fa fa-plus" style="padding-right:5px"></i>Ekle</a>
                            <table class="table table-bordered table-striped table-condensed" id="TblVeriler" style="font-size:13px;margin-top:5px">
                                <thead>
                                    <tr>
                                        <th>Video</th>
                                        <th>İşlemler</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (ViewBag.VideoListesi != null)
                                    {
                                        @foreach (var item in ViewBag.VideoListesi)
                                        {
                                            <tr>

                                                <td>
                                                    <video src="@Url.Content(item.DosyaYolu)" alt="" width="125" height="125" />
                                                </td>

                                                <td>
                                                    <a class="btn btn-danger btn-xs" href="/AdminAnaSayfaVideo/Sil/@item.Id"><i class="icon-trash "></i></a>
                                                </td>
                                            </tr>
                                        }
                                    }

                                </tbody>
                            </table>
                        </div>
                    </section>
                </div>
            </section>
        </div>
    </div>
</section>

